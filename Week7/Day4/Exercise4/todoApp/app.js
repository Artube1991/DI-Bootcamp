class ToDoLoist {
    constructor(task, status) {
        this.task = task;
        this.status = status;
    }
    addTask(task, status) {
        task_list = [];
        task_list.push(value);
        console.log(task_list);
    }

}